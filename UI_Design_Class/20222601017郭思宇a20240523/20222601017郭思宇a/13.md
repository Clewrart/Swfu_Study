1.弹窗输出 alert()
2.文档流document.write(),将内容写入页面,可实现动态修改
eg <h1>Welcome to visit + <script>document.write(name);/script> </h1>
3.多类型输出console.log()
(在调试工具)
4.var const 和 let 区别：var声明的变量在函数外部声明，具有全局作用域。let和const声明的变量具有块级作用域，仅在声明它的代码块内有效。
5.stringto_目标元素__，将文本串转换为它表示的html element……

eg 
在body外
<script>
    var span1 = document.getElementById("span1");
    var button1 = '<button type="button">LLLLL</button>';
    span1.innerHTML = button1;
</script>

在body中
<h1>Welcome to visit
        <span id="span1"></span>
    </h1>

    将在Welcome to visit后有个LLLLL的按钮

Work1：动态创建一个按钮，一个div，一个a标签


6.JS事件交互
<h1 onclick="函数名（参数）">Welcome</h1> 点击此h1元素执行函数名对应函数的行为
<script>
    function 函数名（参数）{
        行为
    }
</script>
Work2:创建3个按钮：Red、Black、Yellow，点击后改变背景颜色
Work3:
Work4:将前面的计算器页面实现功能。
<!--if (xu = "jia") {
    c = a + b;
    document.getElementById("out") = span;
} else if (xu = "jian") {
    c = a - b;
    document.getElementById("out") = span;
} else if (xu = "cheng") {
    c = a * b;
    document.getElementById("out") = span;
} else if (xu = "chu") {
    c = a / b;
    document.getElementById("out") = span;
}
}-->